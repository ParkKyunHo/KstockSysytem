import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export function TradeEvents() {
  return (
    <PlaceholderPage
      title="거래 이벤트"
      phase="P5.3"
      description="DataTable + Filter (event_type / from_date / stock_code) -- audit trail"
    />
  );
}
