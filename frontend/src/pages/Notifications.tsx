import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export function Notifications() {
  return (
    <PlaceholderPage
      title="알림 센터"
      phase="P5.3"
      description="Filter + InlineNotification 다수 + Toast 실시간 (WebSocket P5.4)"
    />
  );
}
