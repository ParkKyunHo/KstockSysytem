import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export function TrackedStocks() {
  return (
    <PlaceholderPage
      title="추적 종목"
      phase="P5.3"
      description="DataTable + TableToolbar + 신규 종목 등록 Modal (PRD Patch #3 -- 경로 선택 UI 없음)"
    />
  );
}
