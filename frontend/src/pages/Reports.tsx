import { PlaceholderPage } from '@/components/shell/PlaceholderPage';

export function Reports() {
  return (
    <PlaceholderPage
      title="리포트"
      phase="P5.3"
      description="DataTable (상태별 액션) + 새 리포트 Modal + 리포트 읽기 Tabs (PART 1/2 Markdown)"
    />
  );
}
