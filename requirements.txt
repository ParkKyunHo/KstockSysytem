# ========================
# K_stock_trading 의존성
# ========================

# HTTP 클라이언트
httpx>=0.25.0
requests>=2.31.0

# WebSocket
websockets>=12.0

# 설정 관리
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# 데이터베이스
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary,pool]>=3.1.0  # PostgreSQL 비동기 드라이버 (Windows 호환)
aiosqlite>=0.19.0  # SQLite 비동기 드라이버

# 로깅
structlog>=23.2.0

# 데이터 처리
pandas>=2.0.0

# 비동기 유틸리티
tenacity>=8.2.0  # 재시도 로직

# ========================
# V7.1 Phase 5 — Web Backend (FastAPI)
# ========================
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-jose[cryptography]>=3.3.0  # JWT
passlib[bcrypt]>=1.7.4              # bcrypt password hash
pyotp>=2.9.0                         # TOTP 2FA
python-multipart>=0.0.9              # form data (FastAPI security)
asyncpg>=0.29.0                      # PostgreSQL 비동기 (V7.1 prod)
slowapi>=0.1.9                       # rate limit

# 개발 도구
mypy>=1.7.0
ruff>=0.1.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# 타입 스텁
types-requests>=2.31.0
pandas-stubs>=2.0.0
